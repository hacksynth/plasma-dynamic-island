import org.kde.plasma.configuration

ConfigModel {
    // No user-facing config pages yet. This file exists so Plasma loads
    // main.xml (for timer state persistence via Plasmoid.configuration).
}
